project_id = "bilvefwxklbgkplzenlr"

# The analytics (vector/logflare) service bind-mounts the Docker socket, which
# the colima runtime does not support ("operation not supported" on docker.sock).
# It is not needed for local development, so disable it to let the stack boot.
[analytics]
enabled = false
